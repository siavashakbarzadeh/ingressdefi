import { AppRouterPipe } from './app-router.pipe';

describe('AppRouterPipe', () => {
  it('create an instance', () => {
    const pipe = new AppRouterPipe();
    expect(pipe).toBeTruthy();
  });
});
